# r193 matched-replay operator-identity stop

The requested tier-8 old/ported replay was stopped before measurement.  The
repository contains the complete projected-Heun CPU owner and validated Metal
component consumers, but no live Metal entry point that executes the complete
authenticated R0/R1/R2 schedule.  The available r182 and r183 diagnostics are
explicitly rejected substitutes.

`stage0_operator_identity_stop.v1` records the source enumeration, exact source
hashes, independent review result, and the requirements that must be satisfied
before a matched replay can acquire authority.  No GPU replay, port verdict,
integration, tolerance change, or golden-checkpoint mutation occurred.
