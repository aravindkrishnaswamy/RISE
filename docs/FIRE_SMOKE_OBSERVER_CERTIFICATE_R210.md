# r210 — observer working-set certificate, not a commuting defect

Status: repair implemented; exact-commit qualification and independent review
pending. No resumed focusing verdict or migration authority is claimed here.

## Measured clause, from the retained state

The reporting-only diagnostic replayed the unmodified step-1300 checkpoint
`2422002e0d45746989b1fa3676f1f4027c785e91bec6b99c3d88b9b01ef12bb2`, at
2.1080244191689417 s. It reproduced the refusal in the observational second
owner, before live solver publication. Its canonical source packet SHA was
`a0c9d80af2810eba79e3ddc9e4fc4c68ea65f25c614f6205643aee203d2f1681`, identical
to r208's staged packet. The checkpoint SHA was unchanged after the probe.

| Clause | Measured quantity | Certificate, same units/scope | Result |
| --- | ---: | ---: | --- |
| Full Metal allocation peak | 8,250,048,512 bytes | 5,584,011,264 bytes | refuses, 2,666,037,248 bytes over |
| Maximum commuting residual | 3.5762786865234375e-7 kg/m³ | 8.9416380433249287e-6 kg/m³ | passes |

There were 19 observer trace stagings. The commuting scale was
1.1719894409179688 kg/m³. The original gamma-128 bound, scalar/momentum
arithmetic, and production certificate are unchanged. This finding is **not**
a per-face port defect. The unqualified reporting probe exited 94 because its
failed-run result lacked a case identity for sidecar publication; the clause
is present in its log, not claimed as a successfully sealed run.

Reporting log SHA:
`14ec9789a2093d9de67379c15f7d1db564097179558f93f293f17a5616754cb4`, in
`rendered/fire_production_calibration/r210_certificate/reporting_only.log`.
These are exploratory measurements, not an exact-commit qualification receipt.

## Derivation and lifetime boundary

Each convergence capture stages 27 cell float fields, 36 face float fields,
one boundary float field, two boundary byte fields and two uint64 identities:

`packet_bytes = 4 * (27*C + 36*F + B) + 2*B + 16`.

For the tier-eight shape, C=504666, F=1533387, B=38778. The packet is
275,544,340 bytes, rounded to the existing 16-KiB allocation quantum as
275,546,112 bytes. The observer certificate is therefore **5,859,557,376
bytes**. This is a layout derivation, not a cap fitted to the observed excess.

Completed autoreleased diagnostic command buffers retained staging resources
until the outer owner returned. A local autorelease pool now drains each
completed capture before the next capture: at most one observer packet is live
in Metal. Host trace vectors remain diagnostic-only and scale with trace count;
the device certificate is not a bound on those host vectors or process RSS.
Allocation and certificate share one packet-layout function.

Production retains precisely the previous certificate function. Only
`qualificationCaptureIterationTrace` selects the observer certificate; the
production resident-step entry refuses that flag before allocation and cannot
publish an observer result. It receives neither a larger cap nor interstage
host substitution. The unsupported-platform API still refuses honestly.

The regression gate pins the production tier-eight certificate, independently
checks the observer layout increment, checks each owner's selected certificate,
and invokes the production entry with only the observer selector mutated.
That request must refuse atomically with no token, identity, or trace staging.
The existing per-cell/per-field r190 owner comparison and residency REDs remain
the numerical/lineage gates. Generated mirror changes are declarations and
source-manifest identities only, not a new reference algorithm.

## Exploratory repaired replay and remaining sequence

The repaired isolated probe accepted eight steps through 2.1211945469258353 s,
ending at 7.2320051193237305 m/s. The formerly failing convergence observation
reported terminal bit identity and wrote its complete trace. Probe log SHA:
`cf9509831cd608bba5cc7a13c8ba69eb902f86d5e38de8409e0013e25400eeb6`.
It explicitly has **no migration authority**, no shortened-prefix verdict,
and no fixed-k decision.

Next: qualify/review the committed repair; prove the old/new resident
continuation under r78; author a new continuation protocol for 3.5 s or the
first crossing. Never rewrite the r194/r208 from-zero protocol to pretend it
already permitted this continuation. Preserve the original run and checkpoint.
The baseline remains 63.96 m/s at 2.699 s and aligned advection 11,692.
159.01 is the r193 equal-time context, not a license to compare different times.

The sibling audit found that failed diagnostic attempts also need the derived
case identity to seal refusal artifacts. The harness now retains that identity
as soon as case construction succeeds, without publishing a failed state.
