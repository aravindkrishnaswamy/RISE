# r207 — ask the tier-eight focusing question at r206 cost

Status: launch preparation; no replay outcome or performance improvement is
claimed. The owner accepted r206 and authorized this full replay before further
cost reduction. The r194 protocol remains immutable.

## Pre-registered experiment

Use the reviewed r206 full-Picard numerical owner, from zero, tier eight, seed
1234, with the r194 source-step ceiling and independently selected CFL schedule.
Run to the protocol's three-second endpoint or its first accepted 60 m/s
crossing. This is not a shortened diagnostic prefix. Preserve checkpoints,
source fields and ledgers, exact beginning/end times, and all 15/30/60 m/s
crossing bundles. The output directory must not exist at launch; resume is
forbidden. The golden checkpoint remains untouched.

The baseline reaches 63.960 m/s at 2.6991133776609786 s, with aligned column
advection 11691.86634461989 kg/(m2 s2). Its first 15/30/60 m/s crossing times
are 2.3182910486939363, 2.6965437539038248, and 2.6991133776609786 s.
The physically mapped fixed-column 159.00892323854879 kg/(m2 s2) oracle
observation is class context, not permission to compare different physical
times: an equal-time claim still requires a separate frozen matched-input
probe and oracle composition to the recorded endpoint. No nearest-time value
may be relabelled equal-time.

The binding r194 protocol SHA-256 is
`5578fa19dcd500ff68743f7f1cfeff21d22da43fef48a3ada42df042badf3e3d`.

## Crossing convergence prerequisite

Code inspection found the r202 convergence exporter qualified only on a
fixture; the live resident entry point explicitly rejects its tracing switch.
Launching that executable unchanged would miss the owner's requested crossing
export. Add a tests-only observation at each crossing and fixed-column event:
rerun the identical request through the diagnostic owner with production-stage
identity tokens, require terminal bit identity against the actual resident
step, and export the selected column's per-iteration momentum/velocity data.
The diagnostic output never substitutes for a production state or publishes
into the evolving solver. Failure suppresses the verdict. Qualification
must exercise this exact hookup and its refusal surface before launch.

Inter-iteration deltas are measurements, not truncation-error certificates.
The fixed provisional momentum of a stage must not be mistaken for converged
candidate dynamics; projected momentum and velocity are reported alongside it.
No fixed-k choice is pre-authorized by a zero provisional-momentum delta.

### First review repairs

Fresh numerical/lineage and evidence reviewers found no P1 and two P2 gaps.
The generic r202 Python checker ignored the new crossing verdicts. A distinct
r207 checker now requires the exact new RED family and both positive verdicts,
and binds the synthetic writer's transcript, CSV and event/source inputs.
Historical six-RED acceptance remains unchanged.

### Fixed-campaign execution authority

The r207 Python verifier verifies **one already executed synthetic v3
qualification**. Its externally recorded raw `crossing_gate.v3.log` SHA-256 is
`d63b483a24d459ac154cb18c566ec9e47f466d6c0619e03d31fd7bebe6c7edd0`.
That pinned log binds the published crossing transcript hash, which binds its
CSV, consumed-target payload, identities and event records. The loader hashes
the exact UTF-8 bytes without normalizing line endings. A caller cannot mint
new execution authority by editing evidence and recomputing its declared hashes.
A future run requires a separately recorded, reviewed campaign binding; this
tool deliberately does not self-qualify arbitrary new executions or authorize
physical replay evidence. This follows the r206 execution-supplement authority
pattern, not a general-purpose cryptographic attestation claim.

Second-round CPU mutations demonstrated the former reader accepted jointly
truncated trace/CSV faces, an equal-volume shape with an invalid column,
replaced consumed-target identities, and invalid device/staging counters after
the caller recomputed dependent hashes. All four now have explicit refusal
tests against the fixed execution root. Independent complete-column topology,
stage/iteration ordering, event-schema and numeric checks supplement that root;
semantic validation is not a substitute for execution authority. The historical
checker remains unchanged and still accepts those rehashed bundles.

The executed v3 device tail payload contains only zero increments. The two
small synthetic reduction witnesses establish requested-demand versus terminal
demand semantics, but do not establish nonzero device-to-export capture or a
realized tail-only physical drain. No new GPU execution or solver change is
implied by this CPU-only verifier repair.

The inherited owner adapter derives `tail_cells` and `tail_drained_m3` from
terminal EOS deviation; these are **remaining terminal tail demand**, not proof
of an earlier executed drain. The port also has no separate restoration
projection: its pressure impulse includes the tangent, source and accumulated
tail target. Crossing columns now say `combined_projection_rate` and
`standalone_restoration_rate`; zero in the latter means no separate pass, not
no tail effect. The raw owner tail columns are explicitly named terminal demand.

At diagnostic captures only, an added device-to-host blit copies the last tail
increment of the target actually consumed by that projection, together with
its sealed target identity and correction iteration. Uncorrected base targets
consume no tail increment. The complete little-endian binary32 fields are
persisted and SHA-bound, with per-trace nonzero populations and requested
increment volume `sum(abs(target))*dt*dx^3`. These requests must not be summed
over Picard iterations as realized physical exchange. Realized tail-only
momentum impulse is not separately identified by the combined projection.
The original per-term total momentum budget remains available without falsely
assigning its tail contribution to an independently measured zero channel.

Two regression witnesses contrast this consumed-target reduction with the old
terminal estimator: a consumed tail ending below threshold retains engagement;
a terminal-only excess does not become a claim of an already consumed drain.
The extra diagnostic staging is one binary32 cell field plus one uint64 target
identity per trace. Production's trace-disabled path and private allocations
are unchanged; CPU reference trace members remain empty rather than being
represented as device observations. Generated source manifests are regenerated.

## Three-way decision and engineering isolation

- Non-curing / tier-spanning: the port retains the focusing mechanism through
  the old failure horizon. Stop cost engineering immediately; report the full
  crossing and source bundle, without choosing k or running a window.
- Port regression: divergence relative to the baseline is localized through
  the independently qualified mirrors/kernel sweep before further progression.
- Port superior / curing: require the complete trajectory and equal-time
  operator comparison before claiming the cure. Then select k from measured
  dynamics plus its contract gate, and proceed toward tier-ten onset/windows.

Freeze the launched executable and source identity. Target-assembly work may
proceed independently, but never relink or replace that executable, change its
inputs, or run a competing GPU benchmark/render. Measurements of cost changes
wait for an uncontended device. Numerical semantics or tolerances cannot change
under a class-A label. Each accepted change still requires the r190 owner gate,
residency REDs, exact evidence, and fresh independent review.

r206 cold-prefix whole-owner p95 was 3243.8116672 ms device and 4736.904042 ms
wall. Their difference is 1493.0923748 ms of elapsed residual, not a measured
hashing duration. Prior sampling attributed about 85% of sampled CPU work to
hashing, not 85% of wall time. Keep both quantities distinct and profile its
critical-path contribution before selecting a hashing optimization. Target
assembly's prior measured cost was about 1.59 s per step; the whole-owner
300–500 ms target remains unmet.

## Parallel target-assembly investigation (not implemented or timed)

Read-only inspection of the three sealed r206 processes gives target terms
1614.420 ms/step, 115.316 ms/call, across 126 calls in nine cold steps. These
are inclusive kernel intervals, not an additive whole-step decomposition.
The target's enthalpy evaluation repeats identical compensated `1/T`, `log(T)`,
and temperature powers for each of seven species; heat capacity repeats the
inverse and powers again. The first single-variable experiment will hoist
these pure expressions once per cell into target-only helpers, preserving
each species' segment selection, coefficient multiplication/addition order,
four-word enclosure values, and refusal/obligation behavior. The common EOS
inversion helper remains unchanged. Compiler common-subexpression elimination
and register pressure could make the measured benefit smaller than expected.

Qualification must compare prepared enthalpy and heat capacity separately,
all four words, against the original direct helpers; include enclosure-word
mutants, segment boundaries and invalid temperatures, and zero-divergence
thermo refusal. Target temperature belongs to the transport parent, not the
later EOS candidate. Hoisting needs no new device allocation or lineage
surface; actual full-owner peak and source-derived manifests still re-derive.
An endpoint-table fallback is more invasive: cached segment obligations must
not include an unused endpoint or omit a consumed one. It is not bundled into
the first experiment.

Other r206 inclusive means are EOS evaluation 904.253 ms/step, advective pair
107.583, FCT ratios 34.956, physical flux 17.112, and endpoint-table construction
4.105. Projection internals, force-RHS encoders, blits, and gaps still need
per-kernel attribution; historical r205 projection timing is not a new r206
measurement. No concurrent GPU cost benchmark is authorized while the verdict
run occupies the device.
