# r214 — filtered authority; tier-ten onset before windows

The owner's acceptance of r213 does not promote its face-rate diagnostic to
a pointwise fidelity gate. Under r112/r172, filter before differencing using
the pinned cubic B-spline and physical width 0.04894898570785762 m. The
production-face comparison −184.08273315429688 versus −23.3961840953128
kg/(m² s²), ratio 7.868066536165445, diagnoses residual concentration; it is
outside the filtered contract's pointwise scope. Its evidence remains the
immutable r213 column report, not a new tolerance.

## Independent engineering run

`docs/fire_r214_tier10_onset.v1` pins the adopted, full-Picard resident §3.7
owner, tier 10, from-zero seed 1234, authored duration 3 s, and observation
horizon 3.5 s or the first 15 m/s crossing. The existing 15/30/60 bundle
captures simultaneous crossings if a step crosses several thresholds.
The first crossing terminates after publishing its bundle; it is not a
shortened-prefix survival claim. The tier-eight historical entry points
retain their original tier, duration, and stop policies.

The new entry accepts only the exact protocol SHA. It cannot use the
generic compatible-momentum diagnostic, resume a checkpoint, change its
horizon through an environment override, or import a tier-eight reference
rate. The fixed-column observation is nominated at 2.1079791976176079 s;
its actual step beginning/end are recorded and require exact-time oracle
composition. A crossing before that time does not require continuing the
unstable trajectory just to obtain the later diagnostic.

The conservation, projection, CFL, admissibility, provenance and r170
outlier gates remain untouched. At a crossing the existing isolated
convergence observer replays the accepted step under its observer-only
working-set certificate. Production arithmetic and its certificate do not
change. This increment changes only the test-run entry and evidence scope.

## Formal filtered verdict: prerequisites, not invented allowances

The r213 /8, /16, /32 oracle schedules start from the oracle's own trajectory.
They are not shared-input production/oracle comparisons. Neither filtering
their endpoints nor dropping the face diagnostic creates the missing
additive terms. The contract remains

`T = Eh,P + EΔt,P + Eh,O + EΔt,O + B32`.

The historical golden fixture's `Eh,P` and temporal constants calibrate the
old remap (`AdvanceResidentStep64`), not the adopted §3.7 owner. The latter's
reviewed fp64 owner exists, but its checkpoint adapter and dyadic calibration
are not yet wired. A new shared represented-input artifact must bind both
owners, the precision conversion, source policy and exact endpoint; then
the owner-specific spatial and temporal terms and B32 must be measured.
The production-oracle distance itself cannot supply its own allowance.

Until then all nine filtered conservative channels, velocity, and nine
inventory-density channels have status **not qualified**. This is neither
a numerical pass nor a numerical failure. r213 accumulated distances remain
diagnostics on separate trajectories. No old-remap bound or historical
single-channel plateau exemption is silently transferred to the new owner.
This formal work does not gate the independent onset experiment.

## Cost track

Accepted r213 uncontended hot means are 6.420787532 device and 8.318470054
wall s/step; p95 is 6.918187667 device and 8.938276375 wall s/step. The
1.897682522 s mean wall-minus-device gap is unattributed, not a measured
hashing duration. Inclusive target assembly is 2.208697209 s/step. These
overlapping inclusive intervals are not an exclusive cost decomposition.

The captured column velocity trace nominates R0/R1/R2 = 5/3/5, already the
full owner's count at that state. Its provisional momentum is a frozen
projection input; zero change there does not prove dynamics convergence.
Fixed-k qualification needs the actual next-momentum trace and discharged
local/class envelopes. No fixed-k variant is enabled or claimed by this
increment. The targets remain ≤1 s/step for the overnight tier-eight window
and 300–500 ms/step for the design budget; neither is met.

Gate/review/run receipts follow in immutable evidence after execution.

## Pre-run review corrections

The first fresh review found a horizon/crossing precedence bug shared by the
onset wrappers: reaching the horizon on the first crossing step could return
success. Survival now explicitly requires both reaching the horizon and no
stop-threshold crossing. The trajectory must contain every expected accepted
step, at its exact schedule time, before summary publication; partial final
writes cannot be sealed as a complete run. Accepted/retry stream closes are
checked and an I/O failure cannot enter ordinary timestep retry.

The same review found that horizontal extrema were rejected before budget
publication. A new staggered-column companion records each momentum term on
all orientations surrounding the observed vertical cell column, including
the actual horizontal extreme. The historical vertical `.column.csv` remains
unchanged in schema. The convergence observer already records all three face
orientations; boundary extrema select the adjacent interior cell. The new
companion and simultaneous-crossing aliases are mandatory published evidence.
These are instrumentation corrections, not changes to production arithmetic.

Named REDs cover a crossing on the horizon-reaching step, missing or duplicate
trajectory rows, a wrong exact time, a failed close, and horizontal/boundary
face indexing and term selection. A fresh review of the corrected state is
required before launch.
