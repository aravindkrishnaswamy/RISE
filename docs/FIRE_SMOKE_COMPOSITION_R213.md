# r213 — same-tier composition and production cost, separate authorities

The r212 oracle reference is accepted. Engineering adoption of the resident
§3.7 owner stands independently of the pending formal fidelity verdict.

## Pre-registered composition measurement

The reference beginning checkpoint is SHA
`ab91898e0279a347e85983853e8fe4167113a2364cdcffb6bad78e327d4e03bf`,
tier eight, at exactly 2.1080244191689417 s. Its producing build is
`35c4f59e4d603eebcad2c37d67860a018b0e13266b6ff7f4807f0c3667d30feb`.
The recorded production step ends at exactly 2.1096706851385534 s.
The oracle diagnostic composes to that endpoint with nominated maximum steps
of the interval /8, /16 and /32. CFL and ordinary refusal reductions remain
active; every accepted substep, exact absolute endpoint, checkpoint SHA, and
full canonical source packet field is retained and sealed. Successful substeps
witness convergence at those inputs; the nominated ceiling is not an assumed
global contraction certificate. A failed endpoint is not silently accepted.

The oracle's diagnostic column is (30,33), the production observation's actual
tier-eight column. Historical callers retain their explicit/default (38,42)
tier-ten column. Rates have units kg/(m² s²); time-integrated advection over
the composed interval divided by that interval is the comparable rate. The
tier-ten 159.01 number is not a tier-eight threshold. Neither the maximum
over the column nor the rate at the velocity-owning face may be substituted
for the other without labelling it.

This first composition is on the oracle's own from-zero trajectory. Equal
time and tier do **not** imply equal beginning field bytes. It supplies a
same-tier reference trajectory and column context, not by itself the shared-
input local scheme/B_fp32 gate. The additive claim still requires its shared
input comparison, ported-operator spatial/temporal terms and fp32 subdominance
with per-quantity, identically scoped units. The older remap's hard-coded
golden fixture/temporal terms are not automatically evidence for this owner.
Temporal studies use the existing r112 SHA-pinned filter before differencing.
No extra tolerance, floor fit, or pooled acceptance ratio is introduced.

The diagnostic reads the old checkpoint but cannot authorize a production
migration. It refuses a changed checkpoint/build, mixed precision, wrong tier,
wrong exact endpoint, production-mode substitution, and an unscoped timestep.
The original checkpoint and all historical evidence remain unchanged.

## Cost work, current authority

r212 already reconciled cold r206 device p95 3.244 s against hot continuation
20.678 s (wall p95 22.959 s). The one observer is outside the production rows.
Hot EOS is 13.547 s/step inclusive, target assembly 2.340 s/step inclusive;
these intervals are not asserted to be exclusive. The average wall-minus-device
residual is 2.072 s, not a measured hashing duration. The 300–500 ms target is
unmet. At the recorded mean, the 25 s projection is about 77 wall hours.

The captured column nominates (5,3,5), not a qualified fixed-k variant. Its
frozen provisional-momentum input cannot establish iteration convergence.
Fixed-k must have identity-bearing stage counts, current-target terminal
projection, the same monitored/r170 physicality policy, recorded truncation
residuals and the r201 matched full-field/class-envelope gate. Oracle-scope
iteration convergence is not silently reclassified as a production acceptance
bound. The full owner remains selected until that qualification passes.

The first new class-A candidate hoists temperature-only compensated EOS
primitives out of the seven-species loop. Species ordering, segment predicates,
four-word values/enclosures and inversion search remain unchanged. A source-
extracted old/new CPU differential test and a wrong-power mutant cover the
local transformation; qualification builds additionally compare the unshared
device arithmetic. Device owner identity, hot eight-step roots, exhaustive EOS
qualification and three independent timing runs are required before promotion.
CPU matches alone are not a Metal qualification or performance claim.

## Measured composition, not yet the additive verdict

The /8, /16 and /32 reference compositions all accepted their full schedules
and reached the exact endpoint. Their column maxima of the time-averaged
signed advection magnitude are respectively 275.43259337063444,
275.4150560292813 and 275.4107054183264 kg/(m² s²). The production column
maximum is 184.08273315429688 in the same units. **The 0.6683935284 ratio is
descriptive only:** the oracle maximum is at z-face 88, whereas production's
is at z-face 33. At face 33 the /32 oracle is -23.3961840953128 and production
is -184.08273315429688. At face 88 the oracle is +275.4107054183264 and
production is -65.64199829101562. A maximum over a different face is not an
aligned-face acceptance criterion. All 107 face rates remain in the artifact.
No tier-ten 159.01 threshold or formal three-way pass is claimed.

The native filtered diagnostic preserves the historical cubic B-spline
primitive, exact cell-integral weights and width D*/5 =
0.04894898570785762 m. Its old helper assumed the analytic 4×4×6 D* domain;
that default remains unchanged. Burning-domain diagnostics instead use actual
checkpoint dimensions times cell width, with interior sample counts derived
from the kernel's two-width support on both sides (39×39×62 here). Applying
the analytic domain lengths to the burning grid would rescale the filter and
is refused by the geometry fixture. This domain extension is diagnostic,
not automatic inheritance of old spatial-contract bounds.

Exploratory filtered /8–/16 versus /16–/32 differences are
0.0019403897741448897 versus 0.0010119867395659468 J/m³ for sensible energy,
and 1.0416656655103438e-5 versus 5.6465786132446363e-6 m/s for vector RMS
velocity. Per-component field and inventory-density differences are retained,
including tiny soot/char channels that do not contract. No universal plateau
certificate is inferred from those two differences. The full shared-input
ported-owner scheme/temporal/B_fp32 gate remains pending; these measurements
must not be relabelled as its completed terms.

Review repaired two diagnostic refusal surfaces: every request field and the
summary's schedule SHA are checked (including re-sealed metadata mutants), and
both momentum-budget streams must close successfully before their output can
be reported accepted. Published sidecar schema, case identity and duplicate
JSON keys are checked too. A synthetic different-face maximum explicitly
cannot become a formal pass.

The authenticated native same-time beginning comparison reports filtered
velocity RMS distance 0.37068117162836201 m/s and sensible-energy mean absolute
distance 3801.9729478659974 J/m³. Its checkpoint SHA pair is the accepted r212
reference and r208 step 1300; neither is rewritten. These are accumulated
separate-trajectory distances, not locally derived scheme allowances.
Artifact `matched_beginning_fields.qualified.v1` SHA:
`ec176997a2577ee21d61f013c4664dd3f1c1e54f2e262eba5cdf46b45657867e`.
The two refined field artifacts have SHAs
`45fadcf050940577ef1a2e16d4d5596d49a59eae4f3383563778042ed78b500a`
and `94a53e4023000ddaef4cb6acd6ecb483fae2545c1a24fb42c70264710b54a5dc`.
They bind the reporter executable independently of the checkpoint producers.

## Hot EOS cost observation and byte-qualification boundary

Three isolated processes, eight accepted steps each, measured device
mean/p95 6.420787532/6.918187667 s and wall mean/p95
8.318470054/8.938276375 s. Against the matched r212 hot processes, the mean
speedups are 2.573× device and 2.217× wall. Inclusive EOS work falls from
13.547402385 to 3.595528156 s/step; target assembly is 2.208697209 s/step.
The 1.897682522 s average wall-minus-device residual remains unattributed.
At this measured mean and the audited step, 25 simulated seconds still projects
to about 35 wall hours, before changing-state costs and frame output. The
300–500 ms target, fixed-k qualification and complete window remain unmet.

Cost observation SHA:
`2b2a597b46759b6e2ea2ef8722996c6463ba8ca4e1053c97d72ff4863c14d600`.
The 49,512,449-input EOS study is the qualified logarithm lattice and midpoint
domain, not 49.5 million independent mixture states. Its log SHA is
`2525e547997dfdac9c90bfad3350461bc8fae5e887c25f823843c1cf9bbd4024`.
The owner and per-cell EOS gates pass, and all eight physics-summary rows
reproduce across each process; full numerical-byte promotion is separate.

The diagnostic snapshot comparison uses the native checkpoint loader and
canonical payload writer. Only the explicitly reported `producerBuildId` is
excluded in an in-memory comparison; no normalized checkpoint is published or
admitted. All canonical persistent fields, including the source/statistics
ledger and manifold lifecycle, remain compared byte-for-byte, with separate
per-component/per-axis bit-mismatch counts. Nonpersistent timing histories are
not falsely claimed to be present in checkpoints. Kernel-dependent identity
roots are expected to differ across the optimized kernel set; exact old and
new roots must instead reproduce their respective frozen producer runs at
each of eight equal times. This diagnostic cannot issue migration authority.

Second review found zero P1 and one P2 in schedule REDs: a stale summary SHA
masked the intended semantic refusal. The repaired tests re-seal every linked
summary and budget as necessary and assert the exact schedule/endpoint error,
including an internally consistent eight-step trajectory ending too early.
Stale-SHA refusal remains separate coverage. No threshold or expectation moved.
