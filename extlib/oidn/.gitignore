# Build outputs and the install tree are regenerated by build.sh.
# The submoduled `source/` itself stays tracked via .gitmodules at
# the repo root; only build artifacts inside source/build/ are
# transient.
source/build/
install/
