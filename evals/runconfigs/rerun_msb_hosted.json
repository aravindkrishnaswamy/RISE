{
  "//": "Standalone re-measurement of multi_step_build on the hosted models. Uses its OWN runDir -- NOT evals/runs/full_baseline. Sharing the baseline dir would be a no-op (the matrix's resume guard skips any cell that already has a non-empty result.jsonl, so baseline cells cannot be replaced in place) AND would append a partial-matrix record to the baseline's run.manifest.jsonl. To REFRESH specific baseline cells after a scenario fix, delete those cell subdirs under evals/runs/full_baseline first, then re-run the full config.",
  "scenarios": ["evals/scenarios/multi_step_build.json"],
  "providers": [
    { "provider": "anthropic", "model": "claude-opus-4-8", "keyEnvVar": "ANTHROPIC_API_KEY" },
    { "provider": "gemini", "model": "gemini-3.5-flash", "keyEnvVar": "GEMINI_API_KEY" },
    { "provider": "openai", "model": "gpt-5.6-terra", "keyEnvVar": "OPENAI_API_KEY" }
  ],
  "repeats": 3,
  "runDir": "evals/runs/rerun_msb_hosted"
}
