{
  "scenarios": [
    "evals/scenarios/bare_prompt_build_courtyard.json",
    "evals/scenarios/build_ambiguous_scene.json"
  ],
  "//": "Gemini-only by user decision (2026-07-29): the full hosted+local matrix is too expensive/slow for this baseline, local models are deficient on build tasks, and gemini-3.5-flash is the same instrument every manual run of the 72-log arc was measured on -- so single-provider results stay comparable to that prior data. Re-widen deliberately if a cross-provider claim is ever needed.",
  "providers": [
    { "provider": "gemini", "model": "gemini-3.5-flash", "keyEnvVar": "GEMINI_API_KEY" }
  ],
  "repeats": 3,
  "runDir": "evals/runs/bare_prompt_baseline"
}
