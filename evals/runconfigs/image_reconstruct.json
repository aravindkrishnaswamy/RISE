{
  "//": "The image-reconstruction pair needs a vision-capable model. Four of the five local_shootout Ollama models are text-only, so the scenarios stay out of that mixed matrix; qwen3.6:27b has its own image_reconstruct_local.json config. This config is the dedicated 4-provider hosted board.",
  "scenarios": [
    "evals/scenarios/image_reconstruct_single.json",
    "evals/scenarios/image_reconstruct_multi.json"
  ],
  "providers": [
    { "provider": "anthropic", "model": "claude-opus-4-8", "keyEnvVar": "ANTHROPIC_API_KEY" },
    { "provider": "gemini", "model": "gemini-3.5-flash", "keyEnvVar": "GEMINI_API_KEY" },
    { "provider": "openai", "model": "gpt-5.6-terra", "keyEnvVar": "OPENAI_API_KEY" },
    { "provider": "xai", "model": "grok-4.5", "keyEnvVar": "XAI_API_KEY" }
  ],
  "repeats": 3,
  "runDir": "evals/runs/image_reconstruct"
}
