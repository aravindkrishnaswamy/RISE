{
  "//": "The image-reconstruction pair (image_reconstruct_single, image_reconstruct_multi) needs a real vision-capable model to read the attached reference photos -- local Ollama models in this repo's shootout are text-only, so these two scenarios are deliberately NOT in full_baseline / local_shootout (which would silently starve every local column of usable input). This is their own dedicated runconfig against the 4 hosted vision providers instead.",
  "scenarios": [
    "evals/scenarios/image_reconstruct_single.json",
    "evals/scenarios/image_reconstruct_multi.json"
  ],
  "providers": [
    { "provider": "anthropic", "model": "claude-opus-4-8", "keyEnvVar": "ANTHROPIC_API_KEY" },
    { "provider": "gemini", "model": "gemini-3.5-flash", "keyEnvVar": "GEMINI_API_KEY" },
    { "provider": "openai", "model": "gpt-5.6-terra", "keyEnvVar": "OPENAI_API_KEY" },
    { "provider": "xai", "model": "grok-4.5", "keyEnvVar": "XAI_API_KEY" }
  ],
  "repeats": 3,
  "runDir": "evals/runs/image_reconstruct"
}
